import 'package:flutter/material.dart';

class FormWizardThird extends StatelessWidget {
  const FormWizardThird({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("test"),
    );
  }
}
